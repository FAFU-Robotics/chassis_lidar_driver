# 从 0 到 1：建图 + 自主导航完整计划

平台：BUNKER MINI 2.0 + RoboSense Airy 96 线 + Jetson  
场地：实验室大厅  
入口：`bash nav_demo/scripts/autonomy.sh`

**「1」的定义（做完才算自主导航闭环）：**  
人指定一个大厅里的目标 → 车用激光知道自己在哪 → 在局部栅格上 A* 出一条不撞障碍的路 → 低速跟过去，能绕开植物/箱体 → 全程 pose 不冻 → 有录像可复现。

PPT 不是这条路线上的站点。素材已经够用，后面只服务上述闭环。

---

## 我们现在在哪里

把整条路分成 10 站。每站有「完成标准」。

```
[0] 硬件通电          已完成
[1] 人遥控开车         已完成
[2] ROS 实时点云       已完成
[3] TF + 轮式 odom     已完成
[4] MOLA 实时定位      部分完成 ← 能用，急转会冻
[5] 存一张合格全局图    未完成  ← 当前卡点（上）
[6] 载图定位           未完成（没有合格图）
[7] 局部栅格 + A*      干跑完成，未跟车
[8] 实车跟路径         代码有，未验收  ← 当前卡点（下）
[9] 实车绕障           未验收
[10] 可重复演示        未开始
```

**一句话：感知和「只规划不运动」已经过关；合格地图和实车跟线还没有。**  
我们站在 **第 4 站末尾 / 第 5 站门口**。第 7 站的干跑可以当导航预习，但不能跳过 5、8 假装已经闭环。

| 已有 | 没有 |
|---|---|
| 雷达 ~10 Hz、TF、轮式 odom | `qualified.json` 合格图仍是 `[]` |
| MOLA 在线里程计 + LocalMap（慢开可用） | 急转不冻的保证 |
| `obstacle_grid` 120×120、0.10 m | 载入旧图再定位 |
| 官方 A* dry-run（直线或绕行都算过） | 车跟着 A* 走出可见绕行 |
| teleop / `:9100` stick | 成功的 live-avoid 录像 |
| lab/lab2 已隔离（几何不合格） | 闭合 U 的激光轨迹 |

当天 teleop_map4 是目前最好的定位证据：pose **未冻**，8.7 m，LocalMap 1244→7847，quality ≈0.89。那是人开车，不是自主。

---

## 第 0 站  硬件与网络（已完成）

- Jetson 系统 Python 3.10 + ROS Humble；**不用 conda 3.13**。
- CAN（candleLight / gs_usb）、网页遥控 `:9101`、控制口 `:9100`。
- 雷达有线 `192.168.1.102 ↔ 192.168.1.200`，MSOP 6699。
- 遥控优先级高于程序。实车前遥控器可接管/急停。

完成标准：ping 通雷达、CAN 能发、网页能动车。

---

## 第 1 站  人工遥控（已完成）

人能在大厅里把车开到任意位置。这是建图的唯一驱动方式，也是 live 失败时的退出手段。

完成标准：慢速前进、转弯、急停都可靠。

---

## 第 2 站  实时三维点云（已完成）

- `rslidar_sdk` → `/rslidar_points`，约 10–11 Hz，96 线，字段 XYZIRT。
- Airy 是**前向半球**，单帧不是全向大厅。
- 显示闪烁（随机抽点）已修；半帧偶发和「定位漂」不是同一件事。

完成标准：RViz 里持续有点云。不要用原始扫描环判断车是否水平。

---

## 第 3 站  TF 与轮式里程计（已完成）

- `base_link → rslidar`：z=0.365，roll=π/2，yaw=π/2。**不要改。**
- `publish_wheel_odom.py` → `/wheel_odom`，给 MOLA 当运动先验。
- FastDDS 用 UDP（`maps/fastdds_udp.xml`），避免 SHM 锁失败。

完成标准：`map → base_link → rslidar` TF 存在；轮式 odom 在发。

---

## 第 4 站  MOLA 实时定位 / LocalMap（部分完成）

**已做到：** 慢开时 `/lidar_odometry/pose` 有 Hz，LocalMap 会涨，ICP failure 可以是 0。

**未做到：** 急转后 pose 经常冻（质量掉到 0.50 附近、Hz→0）。看起来像点云在漂，其实是位姿钉死。

规则：慢开、弯角停、禁止原地猛拧。`MOLA_MINIMUM_ICP_QUALITY=0.50` 不要私自改低。

RViz：Target Frame = `map`，主看 LocalMap，不要 Raw+LocalMap 糊在一起。

完成标准（进入第 5 站前再验一次）：连续开 3 分钟含两个弯，pose Hz 不掉到 0，质量不长期低于 0.6。

---

## 第 5 站  存一张合格全局图（当前主攻）

lab / lab2 已进 `maps/archive/banned_maps/`，**禁止当定位图**。

操作：

```bash
bash nav_demo/scripts/autonomy.sh mapping   # 你遥控慢开
bash nav_demo/scripts/autonomy.sh save      # 写入 maps/runs/hall_时间/
```

合格才写入 `maps/qualified.json` 的 `maps` 列表。合格指：

- 存盘时 pose 没有冻
- 墙/通道在 LocalMap 里对得上现场，不是一条线、不是重影大厅
- `.mm` 与 `.simplemap` 成对存在

完不成第 5 站，第 6 站不存在。导航可以暂时靠第 4 站在线里程计（见第 7 站预习）。

---

## 第 6 站  载图定位（第 5 站之后）

```bash
# 仅当 qualified.json 已有条目
bash maps/start_mola_localization.sh <mapId>
```

完成标准：重启 MOLA 后，车在大厅里慢移，pose 仍钉在同一张图上，不重新把开机点当原点乱漂。

---

## 第 7 站  局部栅格 + A* 干跑（算法已过，实车未接）

```bash
bash nav_demo/scripts/autonomy.sh sense
bash nav_demo/scripts/autonomy.sh status
bash nav_demo/scripts/autonomy.sh plan --forward 1.8
# 或  bash nav_demo/scripts/autonomy.sh plan --gx <x> --gy <y>
```

- 栅格：120×120，0.10 m。unknown 多是正常的。
- 默认向前 1.8 m 若空，结果就是直线——这是对的。
- 要看绕障：把车摆到障碍侧前方，或给会被 occupied 挡住直线的 `--gx --gy`。

完成标准：`found=True`，occupied/inflated hits=0。  
**本站不发 CAN。**

---

## 第 8 站  实车跟路径（下一件要在现场做的）

代码：`run_live_avoid.py`。入口：

```bash
bash nav_demo/scripts/autonomy.sh live-avoid --i-allow-motion
```

没有 `--i-allow-motion` 不会动车。

约束（先不改，用真实失败指导要不要改）：

- `v ≤ 0.06 m/s`
- 横向偏离 0.25 m → ESTOP
- pose 超过 0.50 s 无更新 → 当过期停车

历史上容易在 0.25 m / pose 过期处停住。第一次允许运动前：场地清空、遥控急停在手、先完成第 7 站 found=True。

完成标准：车从 START 跟到 GOAL（哪怕 2 m 直线），全程 sent_can=1 且不是 ESTOP 结束。

---

## 第 9 站  实车绕障

第 8 站直线跟住以后：前方或侧方放真实植物/箱体，A* 出绕行，再 live-avoid。

完成标准：肉眼看到车绕开，RViz 绿线与实车一致，hits=0，有双路录像。

---

## 第 10 站  可重复演示（「1」）

同一场地连续成功 2 次：定位不丢、规划成功、跟线/绕障、急停仍可用。  
可选：载入第 5 站合格图做第 6 站，再重复 7–9。  
此时才叫自主导航闭环，而不是「会规划的遥控车」。

---

## 现场执行顺序（从今天接着干）

不要回头改 PPT。不要改正式 YAML。

1. 预检：雷达 Hz、CAN、遥控急停、`autonomy.sh status`。
2. **第 4 站复验**：慢开两个弯，确认 pose 不冻。冻了就停车等质量，不要硬转。
3. **第 5 站**：mapping → 慢绕大厅 → save → 人看图，合格才改 `qualified.json`。
4. **第 7 站**：sense + plan（先 1.8 m，再有障碍的目标）。
5. 你明确说「允许动车」之后才 **第 8 站** live-avoid。
6. 直线成功再 **第 9 站** 绕障。
7. 两次成功即 **第 10 站**。

失败也留下：起止 pose、found、hits、是否冻、是否 ESTOP、原因。放到新目录，不要覆盖 `ppt_materials/generated/final_20260904/` 定稿。

---

## 明确不做什么

- 不把 PPT 绕行图说成默认 1.8 m 规划。
- 不把激光 L 形轨迹说成闭合 U。
- 不加载 lab/lab2。
- 不在 pose 冻着时继续建图或跟车。
- 不擅自降低 ICP 阈值、放宽 0.25 m、加快 0.06，除非第 8 站用现参反复失败且原因写清楚。
- 不并行开第二套 CAN / 第二套避障抢雷达口。

---

## 相关文件

| 文件 | 用途 |
|---|---|
| `nav_demo/scripts/autonomy.sh` | 唯一操作入口 |
| `AUTONOMY.md` | 命令速查 |
| `maps/README.md` | 点云漂、目录、合格图规则 |
| `maps/qualified.json` | 合格图名单（现在是空的） |
| `nav_demo/plan_once.py` | A* 干跑 |
| `nav_demo/run_live_avoid.py` | 实车跟线（会发 CAN） |

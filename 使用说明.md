# Bunker Mini + RoboSense Airy 工控机联调说明

整理时间：2026-08-24（更新：云端模式已废弃，默认只用本地控制台）  
工控机：`fafu_robot`（Jetson）  
底盘：Bunker Mini 2.0（CAN 500 kbps）  
雷达：RoboSense Airy（UDP `192.168.1.200:6699`）  
USB-CAN：candleLight（`1d50:606f`）→ 内核驱动 `gs_usb` → socketcan（自动检测 `can0/can1`）

---

## 一、项目现状与目录分工

本目录 `chassis_lidar_drivers/` 是**工作区根目录**，项目主体位于 `bunker_jetson/`，
雷达相关代码由组员维护、位于根目录同级。分工如下：

| 路径 | 说明 | 维护 |
|------|------|------|
| `bunker_jetson/` | 本地控制台、导航与控制、轨迹录制/回放、避障等（云端通道已废弃） | 林 |
| `robosense_airy/` | Airy 雷达驱动（WRS 框架版：airy_driver / pcap_reader / pcap_replay / viz / reactive_avoid） | 蔡 |
| `bunker_mini/` | Airy 雷达纯 Python 模块子集（lidar / terrain / obstacle / vision / approach / pcap / ascii_view） | 蔡 |
| `view_lidar.py` / `live_airview.py` / `airy_ros2_bridge.py` | 雷达视图 / 3D 视图 / ROS2 桥 | 蔡 |
| `teleop_desktop.py` / `start_teleop_client.*` | 网页控制台桌面客户端（pywebview 窗口） | 林 |
| `teleop_client_laptop/` | 笔记本双击客户端（拷到本机后双击 `.bat`） | 林 |
| `install_local_service.sh` | 开机无头服务 bunker-local（:9100 + :9101） | 林 |
| `gs_usb_kmod/` | USB-CAN 内核模块源码与构建产物 | 硬件 |
| `tracks/` | 轨迹数据（旧目录，正式数据在 `bunker_jetson/tracks/`） | - |
| `README.md` | 目录分工速览 | - |
| `使用说明.md` | 本文档（联调说明） | - |

> **主路径**：开机服务 `bunker-local`（`bash install_local_service.sh`）或仓库根目录
> `python3 run_local.py`。`fo` / `goto` / `r` / `f` 经 TCP `:9100` 下发（agent **自带** 9100，
> 不要同时开 `bunker-teleop.service` 或第二个 `run_local`）。浏览器打开 `:9101` 可遥控并下发任务。
> **网页遥控专文**：见 [`网页遥控说明.md`](网页遥控说明.md)（WASD、登录、分页、100Hz、常见问题）。
>
> **老师要求的 100Hz / ≤20ms 遥控**：工控机只跑 `run_local.py`；笔记本浏览器
> 打开 `http://<工控机IP>:9101`，或把 `teleop_from_laptop.py` 拷到笔记本，
> 用**本机 PowerShell**跑 `python teleop_from_laptop.py --host <工控机IP>`。
> Cursor 远程终端里的 `kb` 是 SSH 字符流（约 10～30Hz），**达不到**指标。
>
> **云端模式已废弃，不建议调用。** 不要再开 `mock_cloud.py`、`run_mission.py`、
> `start_mission.sh` 或 `run_agent.py` 的 WebSocket 云端入口。后续功能默认接
> `run_local.py` / 网页控制台，非必要不要再靠 WebSocket 云端。
> `run_mission.py` 仍会拉模拟云端，只留给旧流程对照，不是推荐入口。

> **注意**：`robosense_airy/` 与 `bunker_mini/` 下的雷达相关代码由组员维护，
> 请勿改动（其中 `robosense_airy/reactive_avoid.py` 为「方式三」扇区反应式避障，
> 由我方功能移植而来，`bunker_jetson/run_obstacle_avoidance.py` 会复用）。

---

## 二、目录结构

```text
chassis_lidar_drivers/            ← 工作区根目录
├── bunker_jetson/                ← 项目主体（所有命令在此目录运行）
│   ├── bunker_mini/              Python 库（协议/CAN/控制/导航/雷达避障）
│   ├── run_agent.py              车侧代理（请用 --local；WebSocket 云端已废弃）
│   ├── mock_cloud.py             已废弃的模拟云端（不建议启动）
│   ├── keyboard_control.py       键盘实时操控（termios，SSH 下可用）
│   ├── record_track.py           轨迹录制
│   ├── play_track.py             轨迹回放
│   ├── view_lidar.py             雷达实时视图/校准
│   ├── live_viewer.py            点云/地图实时图形查看器
│   ├── live_airview.py           雷达 3D 视图（可选，需 panda3d/wrs）
│   ├── run_obstacle_avoidance.py 方式三：点云反应式避障演示
│   ├── socketcan_chassis.py      SocketCAN 底盘适配层
│   ├── drive_bunker.py           底盘最小驱动/诊断脚本
│   ├── agent.conf / start_agent.sh   配置与一键启动
│   ├── bringup_gs_usb_can.sh     拉起 USB-CAN（gs_usb/candleLight）
│   ├── bringup_can0.sh           拉起板载 mttcan can0
│   ├── recover_candle.sh / reset_candle.sh   USB-CAN 重置脚本
│   ├── requirements.txt          依赖清单
│   ├── examples/                 附加工具脚本（demo/get_robot_info/simulate_lunar_terrain）
│   ├── test/  tests/             回归测试套件
│   ├── tracks/                   轨迹保存目录
│   ├── arm_bridge/               机械臂「抓取完成」信号桥接（独立包）
│   └── 使用说明.md / README.md    文档
├── robosense_airy/               Airy 雷达驱动（组员，WRS 框架版）
│   ├── airy_driver.py            UDP 实时驱动（含 PCAP 回放类）
│   ├── pcap_reader.py            MSOP 抓包解析（纯标准库/scapy 后端）
│   ├── pcap_replay.py            离线 PCAP 3D 回放（可选，需 wrs+panda3d）
│   ├── viz.py                    点云 3D 渲染（可选，需 panda3d）
│   ├── reactive_avoid.py         方式三：多扇区反应式避障（纯 numpy）
│   └── airy_6x12_indoor(1).pcap  内置演示数据
├── bunker_mini/                  Airy 雷达模块子集（组员，lidar/terrain/obstacle/...）
├── gs_usb_kmod/                  内核 gs_usb 源码与模块
├── view_lidar.py / live_airview.py / airy_ros2_bridge.py   雷达视图/3D/ROS2（组员）
├── tracks/                       轨迹数据（旧目录）
├── README.md                     目录分工速览
└── 使用说明.md                   本文档
```

---

## 三、每次上电推荐流程

### 本地遥控（老师要求的 100Hz，不走云端）

工控机只开一个窗口，或装开机服务后不用开窗口。笔记本用浏览器或本机 Python 读键盘，不要在 Cursor 里开 `kb`。
开机先看底盘尾部电压表：**低于 24V 先充电**（手册：低于 24V 为低电；低于 25V 应及时充）。

**开机后台开网页（推荐，桌面不弹雷达/地图窗）**

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers
# 若正在手动跑 run_local.py，先 Ctrl+C 停掉（会抢 :9100）
bash install_local_service.sh
```

之后开机自动听 `:9100` + `:9101`。雷达默认关，网页里再点「雷达开」。
不要再手动开第二个 `python3 run_local.py`，也不要启用旧的 `bunker-teleop.service`。

```bash
systemctl status bunker-local
journalctl -u bunker-local -e
sudo systemctl restart bunker-local
bash install_local_service.sh --disable    # 不要开机自启了
```

调试仍可不用服务、在终端开交互窗口：

```bash
# 工控机（本仓库根目录）
cd /home/fafu_robot/Desktop/chassis_lidar_drivers
python3 run_local.py
```

启动后终端会打印局域网地址。打开网页控制台（任选一种）：

- **笔记本双击（推荐）**：把整个 `teleop_client_laptop/` 拷到笔记本桌面。
  本机执行一次 `pip install -r requirements.txt`（pywebview 桌面窗口）。
  把 `teleop_client.conf` 的 `HOST=` 改成工控机 IP，双击 `打开网页控制台.bat`。
  未装 Python 时同一脚本会退回系统浏览器。不要在 Cursor 里双击。
- **工控机**：本目录 `start_teleop_client.desktop`（文件名也可以是 `打开网页控制台.desktop`，同一份），或 `start_teleop_client.sh`。不要再往桌面根目录放副本。
- **手动打开浏览器**：

```text
http://<工控机局域网IP>:9101
```

点一下页面（英文输入），按住 WASD。方块应亮，页面显示「工控机已收网页指令」。

或把 `teleop_from_laptop.py` 拷到笔记本，本机 PowerShell（不要用 Cursor 底部终端）：

```powershell
python teleop_from_laptop.py --host <工控机局域网IP>
```

本窗口可继续输入 `fo` / `goto` / `r` / `f` / `query`。Ctrl+C = 急停退出。

不要同时开 `bunker-teleop.service` 或第二个 `run_local.py`（会抢 `:9100`）。

### 实战 / 任务（走本地控制台，不要开云端）

```bash
# 1) 硬件：USB-CAN、雷达网线接好；底盘上电；遥控器拨到「指令/CAN」
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
bash bringup_gs_usb_can.sh

# 2) 仓库根目录开本地控制台（不连 WebSocket）
cd /home/fafu_robot/Desktop/chassis_lidar_drivers
python3 run_local.py
```

本窗口或浏览器 `:9101` 下发 `fo` / `goto` / 轨迹。雷达出点后再探路。
Ctrl+C = 停车退出。物理急停始终有效。

> 不要用 `run_mission.py` / `mock_cloud.py` 做无人值守。那条链路是已废弃的
> 云端中继，后续功能不要再往上靠。

### 调试（需要键盘时仍用原来的双终端）

```bash
# 1) 硬件：USB-CAN、雷达网线接好；底盘上电；遥控器拨到「指令/CAN」
lsusb | grep 1d50

# 2) 拉起 USB-CAN（socketcan，自动识别 can0/can1）
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
bash bringup_gs_usb_can.sh

# 3)（可选）确认心跳
candump can1        # 接口名以 bringup 脚本末尾打印的为准
# 看到 211#... 后 Ctrl+C

# 4) 启动方式三避障（终端 ASCII 扇区图，SSH 下即可用，无需图形界面）
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
python3 run_obstacle_avoidance.py
```

关闭窗口或 Ctrl+C = 急停。

---

## 四、常用指令与预期输出

### 1. 确认 USB-CAN

```bash
lsusb | grep 1d50
```

预期输出：

```text
Bus 001 Device xxx: ID 1d50:606f OpenMoko, Inc. Geschwister Schneider CAN adapter
```

### 2. 拉起 USB-CAN

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
bash bringup_gs_usb_can.sh
```

预期输出（关键片段）：

```text
Waiting for candleLight USB-CAN (1d50:606f)...
9: can1: <NOARP,UP,LOWER_UP,ECHO> ...
    can state ERROR-ACTIVE ...
    bitrate 500000 ...
    gs_usb: ...
OK: can1 @ 500000. Test: candump can1
Drive: python3 .../drive_bunker.py --action forward
Avoid: python3 .../run_obstacle_avoidance.py --can-channel can1
```

> 注意：USB-CAN 接口名（can0/can1）**重启后可能互换**，以脚本末尾打印的实际
> 接口名为准，不要硬编码。

### 3. 查看底盘心跳

```bash
candump can1
```

预期输出（持续滚动）：

```text
  can1  211   [8]  ...
  can1  221   [8]  ...
  can1  241   [8]  ...
  can1  252   [8]  ...
```

说明：`211` = 系统状态心跳，`221` = 运动反馈。Ctrl+C 退出。

### 4. 低速前进试车（底盘最小驱动）

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
python3 drive_bunker.py --action diagnose     # 只读状态 5 秒（心跳/电压/故障/模式）
python3 drive_bunker.py --action demo         # 前进→停→左转→停（安全低速）
python3 drive_bunker.py --action forward --duration 3 --linear 0.15
```

预期输出（关键片段）：

```text
已打开 CAN: interface=socketcan channel=can1
等待底盘心跳 (0x211)…
心跳 OK | vehicle=正常 | mode=CAN 指令模式 | batt=25.xV ...
已使能 | ...
→ 前进: v=+0.15 m/s, w=+0.00 rad/s, 3.0s
  vehicle=正常 | mode=CAN 指令模式 | ... v≈+0.15 m/s ...
完成，已停车并关闭总线。
```

> 默认速度 0.15 m/s、角速度 0.3 rad/s，Ctrl+C 必定先停车再关总线。
> 自动检测 USB-CAN 通道；也可 `--channel can1` 显式指定。

### 5. 雷达避障（方式三，终端 ASCII 扇区图）

方式三避障为**终端 ASCII 扇区图**输出（`-` 空、`+` 有点、`#` 密集），
SSH 下即可运行，无需图形界面：

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
python3 run_obstacle_avoidance.py
```

只看点云、不控车：

```bash
python3 run_obstacle_avoidance.py --no-obstacle
```

改巡航速度：

```bash
python3 run_obstacle_avoidance.py --can-channel can1 --cruise 0.12
```

> 方式三基础巡航速度已调低到 0.10 m/s（安全性优先），穿缝缓行 0.07 m/s。

如需 **3D 点云窗口**（可选，需 `panda3d` + `wrs`，且在工控机本机桌面）：

```bash
export DISPLAY=:0
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
python3 live_airview.py
```

> **云端实时图形（`map` / `view` / `vl` / `vm` / `vb`）会弹在 Jetson 桌面**：
> `map` 不再把符号图刷在 SSH 终端里，而是打开和 `vm` 一样的桌面地图窗口。
> SSH 会话里 `DISPLAY` 为空时，`live_viewer.py` 会自动探测本机桌面 X server
> （`/tmp/.X11-unix/X0` + `~/.Xauthority`）并把 matplotlib 窗口弹到工控机
> 显示器上。没接显示器才回退中文简图。查看器 Ctrl+C 关闭时会自动下发
> `pc_stream off` 关流，云端不再报 `Set changed size during iteration`。
> 在云端终端按 Ctrl+C 时，若查看器窗口还在运行，**只关闭雷达窗口、云端
> 保持运行**；需退出云端再按一次 Ctrl+C 或输入 `quit`。同时车侧代理的重连
> 退避已优化：云端重启后会自动快速重连（2026-08 修复，不再出现重启云端后
> 「没检测到车侧代理」）。
>
> **Ctrl+C 直接退出云端的根因（2026-08 二次修复）**：旧 `_term_keys.py` 用
> `tty.setcbreak()` 进入终端模式时会**保留 `ISIG` 标志**，导致 Ctrl+C 被
> 终端驱动直接转成 SIGINT → `KeyboardInterrupt` 中断整个 `asyncio` 事件循环，
> `\x03` 字节根本没机会被 `read_char()` 读到。现改为在 cbreak 基础上**额外
> 关闭 ISIG**，Ctrl+C 才会作为 `\x03` 字节进入按键队列，命令模式才能区分
> 「只关雷达查看器」vs「退出云端」。
>
> **终端排版错乱修复（2026-08）**：自定义终端模式初版复刻 `tty.setcbreak()`
> 时把 `OPOST`（输出处理）一并清掉，导致输出 `\n` 只换行不回车，help/日志
> 每一行都从上一行末尾列开始打印（行首随机空格、逐行右移）。修复为保留
> `OPOST` 并置位 `ONLCR`（`\n` → `\r\n`），输入侧 raw（ICANON/ECHO/ISIG）
> 不受影响，功能不变。

---

## 五、网络与接口约定

| 项目 | 值 |
|------|-----|
| 工控机 Wi-Fi / 给笔记本 | `59.79.233.120`（笔记本打开 `:9101` 用这个） |
| 工控机有线网（雷达） | `enP8p1s0` → `192.168.1.102/24`（只给雷达，笔记本不要填） |
| 雷达 | `192.168.1.200`，MSOP UDP 端口 `6699` |
| 板载 CAN | `can0`（MTTCAN，本方案一般不用） |
| USB-CAN | `can1`（gs_usb，底盘控制用；重启后可能与 can0 互换） |
| CAN 波特率 | `500000` |

快速确认雷达：

```bash
ping -c 2 192.168.1.200
```

---

## 六、内核 gs_usb 模块（已安装）

- 源码/构建目录：`gs_usb_kmod/`
- 模块路径示例：`/lib/modules/$(uname -r)/updates/gs_usb.ko`
- 开机自动加载：`/etc/modules-load.d/gs_usb.conf`（内容为 `gs_usb`）

若重启后 USB-CAN 不出，可手动：

```bash
sudo modprobe gs_usb
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
bash bringup_gs_usb_can.sh
```

重新编译（一般不必）：

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/gs_usb_kmod
make -j$(nproc)
sudo make install
sudo depmod -a
sudo modprobe gs_usb
```

---

## 七、故障速查

### USB-CAN 认不出来

```bash
lsusb | grep 1d50
# 若无：拔掉等 10 秒，换机身 USB 口再插
dmesg | tail -30   # 或：journalctl -k -n 40
```

可尝试软恢复（不保证成功）：

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
bash recover_candle.sh
```

仍不行则物理拔插。

### 有 can 网卡但没心跳

```bash
ip -details -s link show can1
candump can1
```

检查：底盘是否上电、CAN_H/CAN_L 是否接对、终端电阻。

### can 网卡已恢复（UP）但总线上仍无任何帧

`recover_can_now.sh` / `fix_candle_usb.sh` 已把 can 网卡拉起（`can1: UP`、
`gs_usb` 已绑定），但 `candump can1` 依然 0 帧、agent 依旧报 `⚠ 无反馈`——
这已不是网卡/驱动问题，而是**物理层或底盘侧**问题。

先跑「节点存在性测试」（决定性验证，需要 root）：

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
sudo bash test_can_bus.sh
```

**这个测试非常关键**：CAN 协议要求发送帧必须被总线上**至少一个其他节点**
ACK。如果 `cansend` 返回 0（成功），说明总线有节点应答 → 适配器与总线的
物理连接是通的，问题在底盘侧（没开机/没广播/波特率不匹配）；如果报
`NO ACK`，说明总线上只有适配器自己 → CAN_H/CAN_L 根本没接到底盘。

再跑完整诊断（需要 root）：

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
sudo bash diagnose_can.sh
```

脚本会检查网卡状态、gs_usb 绑定、主动 `cansend` 测试适配器 TX、
can1/can0 各监听 8 秒、接口收发统计，并给出结论。重点排查：

1. **底盘是否真的开机**（指示灯/风扇）；BUNKER MINI 上电后即每 ~10 ms 广播
   0x211，只要在线就一定能抓到
2. **CAN_H / CAN_L 是否接对**：适配器 CAN_H↔底盘 CAN_H、CAN_L↔底盘 CAN_L，
   接反或松动都会完全静默
3. 部分 USB-CAN 适配器需独立供电，确认供电正常
4. 换一根 CAN 线 / 换一个 USB 口再试

### USB-CAN 插上却一直 DOWN / 小车不动（最常见根因）

`lsusb` 能看到 `1d50:606f`，`ip link show` 却显示 `can1 DOWN`，且
`candump can1` 无任何帧——多半是 `/etc/udev/rules.d/99-gs_usb-candle.rules`
**语法错误**（历史版本行尾多了一个右括号），导致整个规则文件解析失败：
`MODE=0666` 设备权限和「插入即自动 `ip link set up`」的规则全部失效。

一键诊断修复（需要 root）：

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
sudo bash fix_candle_usb.sh
```

脚本会：① 修复损坏的 udev 规则；② `udevadm` 重新加载并触发；
③ 拉起全部 can* 网卡；④ 逐个 `candump` 探测底盘反馈，标出真正连底盘的口，
并给出 `BUNKER_CAN_CHANNEL=xxx` 建议。

### USB-CAN 枚举正常但【没有】生成 can 网卡（只剩板载 can0）

另一种常见情形：`lsusb` 能看到 `1d50:606f`（设备枚举正常），但
`ls /sys/class/net` 下**没有 USB 归属的 can\* 网卡**（只剩板载 can0）。
说明 gs_usb 驱动没绑定设备——通常是设备插入时 probe 时机错过，或设备
被用户态程序（如直接打开过 gs_usb/libusb）卡住。此时 agent 锁任何通道
都收不到底盘反馈，小车不动。

用「立即恢复」脚本（需要 root）：

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
sudo bash recover_can_now.sh
```

脚本会：① 结束占用设备的用户态程序；② `usbreset` 软复位触发重新枚举；
③ 确认 can 网卡已生成并拉起；④ 探测底盘反馈，输出 `BUNKER_CAN_CHANNEL=xxx`。
若软复位无效，脚本会明确提示【物理拔掉 USB-CAN，等 3 秒，再插回】——
这是最终且最可靠的手段。

> 同时注意：车侧代理启动时若没有显式 `--channel`，会先自动拉起所有 can* 网卡，
> 再短听各口的 0x211 反馈帧自动选口；不要用旧脚本手动填死通道。

### 底盘明明在广播 0x211，agent 却听错口 / 小车不动（锁定根因）

**现象**：`find_chassis.sh` 在 can1 上确实抓到了底盘广播
（`211`、`221` 电机转速、`311` 电机位置、`241/252/253` BMS），**底盘在线、
链路正常**；但 agent 日志 `CAN layer initialized (channel=can0)`，小车怎么
发指令都不动。

**根因**：agent 只在**启动时探测一次**通道。如果那一刻底盘还没开始广播
（适配器刚插上还在初始化 / 底盘比 agent 启动慢），探测失败就回退到默认
`can0` 并**永久锁死**——即使之后底盘在 can1 上正常广播，agent 也听不见。

**修复（已内置）**：agent 现在会在**无底盘反馈时每 10 秒自动重探**所有
SocketCAN 网卡，一旦发现某个口有 0x211 反馈就**热切换**到该通道，不再需要
重启。日志会出现：

```
[底盘诊断] 重探发现底盘在通道 can1（当前 can0）——热切换到 can1
```

**验证底盘当前在哪个口**（需要 root，会同时监听 can0/can1 各 15 秒）：

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers/bunker_jetson
sudo bash find_chassis.sh
```

输出里「收到 0x211 —— 底盘在这个口」即表示该口在线。若你明确知道底盘在
can1，也可直接指定，跳过自动探测：

```bash
BUNKER_CAN_CHANNEL=can1 bash start_agent.sh
```

> 关于 `can_util.py` 里的 candleLight 设备发现（`gs_usb`/`candle`/Zadig 驱动）：
> 那是 **Windows 专用路径**。在 Linux/Jetson 上一切走 **socketcan**，无需也
> 用不上那套探测——本机的「自动选口 + 无反馈重探」已覆盖全部场景。

---

## 附：本次「小车不动」CAN 问题完整解决过程复盘（2026-08-13）

**症状**：底盘确认开机、遥控关闭、连线自认正常，但云端发 `goto`/进入
`kb` 模式小车完全不动；agent 日志反复报 `No chassis status received on 0x211`。

**逐层排查与根因**（从软件到硬件，最终定位在「agent 锁错 CAN 通道」）：

| # | 排查项 | 工具 | 结果 | 结论 |
|---|--------|------|------|------|
| 1 | agent 用的 CAN 通道 | `agent.log` 的 `CAN layer initialized` | 一直 `channel=can0` | 通道可能选错 |
| 2 | 板载 can0 vs USB-CAN can1 | `ls /sys/class/net` | 两者都存在 | 需确认底盘接哪个 |
| 3 | 适配器是否卡死 | `find_chassis.sh` 自环 | can1 能收回 echo | 适配器收发正常 |
| 4 | 底盘在哪个口广播 | `find_chassis.sh` 监听 | **can1 收到 `211/221/311/241`** | 底盘在 can1，链路物理正常 |
| 5 | 为什么 agent 锁 can0 | 读 `probe_chassis_channel` 时序 | agent 只启动时探测一次 | 探测时底盘未广播 → 回退 can0 永久锁死 |

**根因**：`probe_chassis_channel` 只在 **agent 启动时执行一次**。若那一刻底盘
尚未开始广播（USB-CAN 刚插入仍在初始化、或底盘比 agent 慢），探测失败即
回退默认 `can0` 并永久锁死；此后底盘在 can1 上正常广播 agent 也听不见，
所有运动指令（goto/kb/move）都发往一个没有底盘的通道，自然一动不动。

**修复（三处）**：

1. `bunker_mini/controller.py` 新增 `switch_channel()`：不重建 recorder/player
   /navigator（它们都持 controller 引用）即可热切换 CAN 总线通道。
2. `bunker_mini/agent.py` 新增 `_maybe_reprobe_can_channel()`：只要从未收到
   底盘反馈，就每 10 s 重探所有 SocketCAN 网卡，发现 0x211 即热切换。
3. 新增 `find_chassis.sh`（定位底盘在哪个口）、`reset_usbcan_firmware.sh`
   （适配器固件卡死恢复），并修复 `find_chassis.sh` 旧版 grep 前导空格
   误判 0x211 的问题。

**验证**（修复后实际运行）：

```
probe: CAN 通道 can1 收到底盘反馈 0x211，选定该通道
CAN layer initialized (interface=socketcan, channel=can1)
控制模式已切到 CAN 指令模式，无故障
```

**经验教训**：`cansend` 返回 0 只能证明「适配器把帧交给固件」，不能证明
「帧上总线 / 底盘收到」；判定底盘在线的唯一可靠依据是**能否收到底盘主动
广播的 0x211**。而判断底盘在哪个口、agent 该锁哪个口，必须用「有反馈的口」
而不是「猜的口」——这也是本次把「启动时一次性探测」升级为「无反馈周期
重探」的原因。

---

## 附：网页遥控已通、底盘仍无响应（2026-08-22～23）

**现象**：Chrome `:9101` 已连接、100Hz、RTT 数毫秒，工控机已收 stick，终端 `mode=?`，车不动。

**思路**：网页「已连接」只说明笔记本→工控机。手册规定底盘上电后 **0x211 每 200ms 自广播**，这才是锁定依据。软件侧曾把总线探成 listen-only、空闲乱发 `0x421` 堵死 USB-CAN 发送队列、页面失焦误急停——这些会让按键发不出去，清掉之后 `candump` 仍空。

**根因**：尾部电压过低。手册：低于 24V 为低电、低于 25V 应充电。欠压时控制器/BMS 保护关机，CAN 节点整段消失（不是频率对不上）。充电后 `0x211` 恢复，遥控正常。

**记住**：先看电压表再开车。`mode=?` 先查电量，再查软件。

---

### 3D 点云窗口找不到

仅 `live_airview.py`（3D 视图）有图形窗口：

- 必须在工控机本机看桌面（远程 SSH/Cursor 终端不会把窗口“画”到你笔记本上）
- `export DISPLAY=:0` 后启动
- 需要已安装 `panda3d` 与 `wrs` 框架
- Alt+Tab 找窗口；方式三 `run_obstacle_avoidance.py` 是纯 ASCII 输出，无窗口

### 模式变成遥控 / 反复报「遥控器失联」

手册：遥控器**没开**时默认待机，发 `0x421=0x01` 才进 CAN 指令模式；遥控器**开着**
权限最高，会挡住 CAN。关机前若停在遥控档，底盘会一直报遥控模式并带失联位
`0x04`，单发一次 `0x421` 会被吃掉。

**2026-08-19 已修**：代理看 `0x241` 还在不在（约 0.5 s 无帧 = 遥控器已关）；
已关却仍报遥控时先 `0x441` 清失联锁，并在 50 Hz 控制环里**保持** `0x421`。
单独的 `0x04` 不再当云端故障事件刷屏。状态栏：遥控器真在线才写「⚠遥控占用」；
已关则写「遥控器已关，正在切 CAN」，一两秒内应变为「CAN 指令模式」。

若仍带「⚠遥控占用」，接收机还在收到遥控帧：把遥控器彻底关机，或把 SWB
拨到最上（指令档）。改完后需**重启车侧代理**。

---

## 八、导航与控制、轨迹功能（本地主路径）

**完整功能与操作说明统一见 [`bunker_jetson/使用说明.md`](bunker_jetson/使用说明.md)**。
日常入口只有本地控制台：

```bash
cd /home/fafu_robot/Desktop/chassis_lidar_drivers
pip3 install -r bunker_jetson/requirements.txt   # 首次
python3 run_local.py
```

> **云端模式已废弃。** 不要再开 `mock_cloud.py`、双终端 WebSocket、
> `start_agent.sh` 连 `ws://127.0.0.1:9000`，或 `run_mission.py`。
> 这些入口仍留在仓库里，只为对照旧代码，**不建议调用**。后续功能默认
> 接到 `run_local.py` / `:9101` 网页，非必要不要再设计云端依赖。

核心能力概览（详见 bunker_jetson 文档）：

| 功能 | 模块/脚本 | 说明 |
|------|-----------|------|
| 本地车侧代理 | `bunker_mini/agent.py` + `run_agent.py --local` | 本地 TCP / 网页指令 ↔ CAN；WebSocket 云端通道已废弃 |
| 任务链远程干预 | `bunker_mini/agent.py` | `cancel`（优雅中止+返回）、`odom_reset`（重置原点） |
| 建图接入通道 | `bunker_mini/agent.py` + `occupancy.py` | `map_upload` / `pose_align` / `map_return`（地图交付即用） |
| 点对点导航 | `bunker_mini/navigator.py` | 里程计航迹推算 + 航点跟随 + 避障绕行 |
| A* 全局规划 | `bunker_mini/global_planner.py` | 在雷达伪地图上绕障 |
| 轨迹录制/回放 | `bunker_mini/tracker.py` | 里程驱动 + 段内插值纠偏 + 轮位移反推速度 + 末端停靠；建议 `r`+WASD 录 |
| 避障守卫 | `bunker_mini/obstacle.py` | 反应式近场保护（不读建图）；`m`/goto/回放过守卫，`kb` 不过 |
| 地形通过性 | `bunker_mini/terrain.py` | 台阶/岩壁/坡/坑区分，复杂路况导航 |
| 2D 伪地图 | `bunker_mini/occupancy.py` | goto 预检 / 地图可视化 |
| 自主巡游 | `bunker_mini/patrol.py` | 未知环境自主探索 + 探索记忆 |
| 目标检测/对接 | `bunker_mini/vision.py` + `approach.py` | 反射率聚类找目标 + 机械臂对接状态机 |
| 机械臂抓取反馈 | `arm_bridge/` | 机械臂「抓取完成」信号桥接（本地指令/CAN/GPIO/文件） |
| PCAP 离线回放 | `bunker_mini/pcap.py` | 无雷达也能跑通避障/导航链路 |
| 方式三反应式避障 | `run_obstacle_avoidance.py` | 复用蔡 `robosense_airy/reactive_avoid.py` |

> **坐标系说明（重要）**：`goto` / `go_home` / 轨迹记录里的坐标都基于**导航原点
> (0,0)**——即本地代理（`run_local.py` / `run_agent.py --local`）启动时小车所在
> 的位置、车头为 yaw=0°。它**不是底盘物理上电的位置**。运行中可用
> `odom_reset / z` 把原点重置为当前位置。

---

## 九、指令处理链路说明（kb 不灵敏 / 断线与任务链 / goto 语义）

### 遥控通道：不要用 SSH 当控制总线

老师要求：直连 TCP、**100Hz**、端到端大约 **≤20ms**；同网 Wi-Fi 6 的 TCP RTT
应是数毫秒到十几毫秒。**50Hz 已经慢**。SSH 当遥控总线只有约 10～20Hz。

**正确用法**

1. 工控机只开：`python3 run_local.py`（agent 听 `:9100`，CAN 按 10ms 下发）。
2. 把根目录 `teleop_from_laptop.py` 拷到笔记本。
3. 笔记本**本机** PowerShell / CMD（不要用 Cursor 底部终端）跑：
   `python teleop_from_laptop.py --host <工控机局域网IP>`

这时链路是：`笔记本 HID 100Hz → Wi-Fi TCP :9100 → set_velocity_now → CAN 100Hz`。
stick 默认**不等 ACK**；`cmd` 异步执行，不会挡住 100Hz。Cursor 只用来看代码、
下 `fo` / `goto`，**不要在远程终端里开 `kb` 开车**。

测延迟（测的是 TCP RTT，不是屏幕刷新）：

```text
python3 bunker_jetson/measure_latency.py --tcp-host <工控机IP>
```

同网 ping 若经常 >20ms，先改无线（5GHz / Wi-Fi 6，靠近路由，或改有线），
不要再调 `TermKeyReader` 的 hold 窗口。`TELEOP_LIVE_S` 加大则松键更慢，
减小则 SSH 更顿——那是终端协议上限，不是参数没调好。

`bunker-teleop.service` 只给「只要遥控、不开 agent」用；和 `run_local` /
agent **同时开会抢 9100/CAN**，不要一起开。`:9100` 默认听 `0.0.0.0`，
token 默认 `bunker-teleop`，局域网可连，不要暴露到不可信网络。

**Cursor / SSH 里的 `kb` 为什么达不到**

本终端只有字符流，没有 key-up。`drive_stick()` 用「静默超过 50ms 清零」猜松开。
循环可以 10ms 空打一帧，真正的状态更新取决于 SSH 送来多少字符（常 10～30Hz）。
首击还有 500～660ms 连发延迟；同一时刻通常只连发最后一键，W+A 是锁存补的。
所以：

| 指标 | 老师期望 | Cursor 里 `kb` | 笔记本 HID → `:9100` |
|------|----------|----------------|----------------------|
| 更新频率 | 100Hz | 输入 10～30Hz | 采样 100Hz |
| 端到端延迟 | ≤20ms | 常见 50～90ms，松键 ≥50ms | 近路由约 12～25ms |
| 多键 / 松一留一 | 要跟手 | 做不到，只能猜 | 可以 |

车侧 100Hz CAN、本机 TCP、`set_velocity_now` 已经对齐老师；缺的是人机接口
不能走 SSH。`kb` **不过**雷达守卫（测避障用 `m` / `goto`）。
已废弃的 `mock_cloud` kb 仍是旧 WebSocket 路径，**不要再开**。低延迟只走 TCP HID。

### 避障在看什么（不依赖建图）

`m` / `goto` / 回放过 `ObstacleGuard`：用**当前帧点云**做扇区距离 + 立体盒
（人体/椅背、近场立柱/椅面、履带外侧），近了就限速或急停。2D 伪地图只给
`map` / `goto` 预检 / A*，带 5 s 衰减，**不参与** `m` 急停。空地曾把雷达
支架/线缆自扫当成「钻进桌椅下方」——已收紧椅面判定并剔除安装件。雷达看不见
的地方（下方、0.1 m 盲区）守卫也看不见。

### 主线导航 / 回放控制周期

遥控 `0x111` 由车侧 TX 环按 **10 ms / 100 Hz** 下发（手册 20 ms 是推荐上限，
500 ms 无帧才停；软件 100Hz 改善的是指令新鲜度，电机环改不了。若拿 `0x221`
反馈周期验收，仍可能看到约 20ms）。`goto` / 巡游 / 对接规划环仍是 **20 ms**。
轨迹回放每 20~50 ms 重发速度。任务速度在 Jetson 本地，不经 SSH。

### 底盘统一速度斜坡（所有模式共用）

所有驱动者（kb / goto / patrol / approach）最终都走
`controller.set_velocity()`：

- `set_velocity(v, w)`：线速度加速 ≤0.5 m/s²、减速 ≤1.5 m/s²；
  角速度加速 ≤1.0 rad/s²、减速 ≤3.0 rad/s²。按真实时间间隔推进。
- `set_velocity_now(v, w)` / `stop_motion()`：**直通**，供 ESTOP、
  遥控看门狗立即停车，以及**轨迹回放**（录制时斜坡已进 0x221，回放不再套第二层）。

### 链路本质区别，以及断线时不该发生的事

| | kb / 手动 `move` | `fo` / `goto` / 巡游 / 回放 / 对接 |
|---|---|---|
| 速度从哪来 | 笔记本 HID / 本窗口 `kb` → TCP `:9100` | LiDAR + 规划 + `_drive` **全在 Jetson** |
| 网络在不在控制环里 | 遥控在（局域网 TCP）；旧 WebSocket 云端已废弃 | **不在**（任务在车侧闭环） |
| 遥控通道断开 | **立即停车**（遥控失控） | **继续跑**（不能把车停在溶洞里） |

旧云端逻辑无论是否在跑任务，WebSocket 断线都 `stop_motion()` 并取消任务。
那是已废弃通道的历史问题；日常不要再开 `mock_cloud`。

**当前（2026-08-17）**：

- 正在跑 `find_object` / `goto` / 轨迹回放 / 运输任务 → 断线**不停车、不取消**，
  只报 OFFLINE，清掉残留遥控 `move`（避免看门狗误刹）。扫描匹配与伪地图
  在车侧继续更新，不跟 WebSocket 一起停。
- 纯 kb/`move` 遥控 → 仍然立即停车。
- 物理急停、本地 `c` / `estop` 语义不变。网页或 TCP 断线期间暂时没有远程
  监视；任务进度仍在车侧跑，连上后会重新报上来。

**说明**：kb 手感差不能推论成「任务也会丢 CAN 帧」。任务速度不经过 WebSocket。
雷达掉线仍走 fail-safe 守卫（速度打成 0），那是安全设计，不是网络丢包。

### goto 的两个参数是什么？为什么路径"歪"

**参数含义**：`goto x y` 的 `x`/`y` 是**目标点坐标**（米），基于导航原点——
不是"前后 x 米、左右 y 米"的**相对位移**。`x`=前方、`y`=左侧，但两者合起来
是**一个世界坐标点**，小车自主导航到该点（途中避障）。

**为什么 `goto 0.5 0` 会左右移动 / 走得不止 0.5 m**：

1. **初始朝向 ≠ 目标方向**：目标方位 = `atan2(dy, dx)`。若小车启动时 yaw 不是
   0°（启动后动过 / 里程计漂移），导航先转向对准再前进 → 看起来左右移动。
2. **A* 全局规划**：`_plan_waypoints` 用雷达伪地图规划绕障折线。伪地图若残留
   历史障碍数据（时间衰减窗口内），可能规划出绕行航点 → 走折线而非直线。
3. **避障绕行**：导航中遇障碍会按锁定方向绕行（`detour_side`）或重规划，
   路线加长。
4. **到达判定是逼近坐标点**：直到距离 ≤ 0.15 m 才判到达，途中 PID 持续修正
   航向。位置来自里程计推算，履带打滑会让推算位移偏大 →「看起来走过头」。

**建议**：
- 想「直行固定距离」，用 `move`（线速度 × 时间）：`m 0.2 0 3` = 0.2 m/s 前进 3 秒。
- 用 `goto` 前先 `query`/`q` 看当前 `pose`（x, y, yaw），再用 `map <x> <y>`
  预检目标点能否通行。
- 若发现 goto 前有伪地图残留障碍导致绕行，可用 `odom_reset / z` 清原点，
  或等伪地图时间衰减自动清除旧障碍。

---

## 附：历史联调问题记录（2026-08-08 首次整理）

下表为早期排查记录，部分问题已随代码修复，仅作参考：

| 问题 | 原因 | 解决方法 |
|------|------|----------|
| 板载 `can0` 无底盘报文 | Jetson MTTCAN 未接底盘；实际用 USB-CAN | 改用 candleLight，走 socketcan |
| 代理启动报 `Network is down` / `OSError` 原始堆栈 | SocketCAN 网卡未开启（`DOWN`） | 已修复：代理会自动尝试拉起，失败则打印可执行命令 |
| USB-CAN 一开始识别不到 | 未插到本机 / USB 口异常 | 直插工控机 USB；`lsusb` 见 `1d50:606f` |
| 用户态 pyusb `gs_usb` 易卡死，要反复拔插 | 内核未启用 `CONFIG_CAN_GS_USB`，TX 超时会卡死固件 | 编译安装内核模块 `gs_usb.ko`，用 socketcan |
| `bringup` 提示找不到 USB-CAN | 适配器卡死后枚举失败（error -110/-71） | 拔掉等约 10 秒，换口再插 |
| “没有点云图” | 窗口开在**工控机本机桌面**，远程终端只显示文字 | 本机 Alt+Tab 找 `RoboSense Airy` 窗口 |
| 车不跟程序指令走 | 遥控器抢成 `REMOTE_CONTROL`，或关机后锁在遥控+0x04 | 关机即可；代理会清失联并保持 `0x421`。仍占权则 SWB 拨到最上 |
| 网页已连接、100Hz，车仍不动（`mode=?`） | 低电（低于 24V）底盘停广播 `0x211`；或软件把 CAN 发送队列堵死 | 先看尾部电压并充电；网页通≠底盘锁定 |
| Cursor 里 kb 一顿一顿 / 达不到 100Hz | SSH 字符流不是 HID，约 10～30Hz | 笔记本本机跑 `teleop_from_laptop.py --host <工控机IP>` |
| kb 松键后仍往原方向冲（旧） | +0.20s deadline / 0.58s 猜键窗 | 遥控 stick 立刻执行；松键靠 HID 发 (0,0) |
| 退出 kb 后终端仍刷状态 | 里程 0.1 m / 地图格子被当成显著变化 | 命令模式忽略这些抖动；`query` 仍可看状态 |
| 空地 `m` 报钻进桌椅 | 雷达自扫进了旧椅面盒子 | 已剔除安装件；重启 agent |

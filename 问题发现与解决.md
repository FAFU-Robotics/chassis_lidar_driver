# 问题发现与解决

记录对话中遇到的问题、判断思路与处理办法：先是把 Windows 版 `bunker_mini_can_control` 移植到 Jetson 并整理为 `bunker_jetson/`（§1–10），随后是云端、kb、CAN、雷达查看、导航与轨迹录放精度（§11 起），再后是云端 kb 多键抓不住本质、延迟来源与 TCP 遥操（§19 起），再后是本地控制台 100Hz、去掉猜键、以及「笔记本连不上 9100」后改走网页（§22 起），再后是网页已通但车不动、一次定位为底盘低电（§24），再后是同表象但根因是 candleLight USB 发送管道卡死、物理拔插恢复（§25），再后是键盘 HID 已趋于可用之后，`goto` 只到点不管车身、`fb` 回起点角速度对不上（§26）。

当前可运行代码以 `bunker_jetson/` 为准。详细用法见仓库根目录 `使用说明.md`。

---

## 1. Windows 代码不能直接在 Jetson 上跑

**现象**：参考工程在 Windows + 用户态 `gs_usb` + `keyboard` 库下工作；工控机是 Linux，接口、输入、依赖都不一样。

**思路**：只复用协议与业务逻辑，把操作系统相关层全部换成 Linux 原生能力，不要在参考目录里改文件。

**处理**：
- CAN：用户态 `gs_usb` → 内核 `gs_usb` + SocketCAN（`can0`/`can1`），通道自动检测，禁止硬编码接口名。
- 键盘：`keyboard` 库在 SSH/无图形界面下不可用 → `_term_keys.py` + `termios` 逐字符读取。
- 入口脚本统一走 `_bootstrap.ensure_project_root()`，保证从任意目录启动都导入本目录的包。

---

## 2. 云端 kb 模式：不灵敏、长按速度抖、组合键松开后停车

**现象**：
- 按键响应慢，不如 Windows 直连 CAN。
- 长按同一方向时速度时快时慢。
- 同时按住两个方向键后松开其中一个，另一个仍按着，车却停住。

**原因**：SSH 下只有按键事件、没有可靠的“松开”事件；终端自动重复间隔不均匀；组合键里某一键短暂丢事件会被当成“全部松开”。

**处理**（`_term_keys.py` / `mock_cloud.py`）：
- 每键做滞回状态机（按下 / 锁存 / 组合保持）。
- 自适应保持窗口：按自动重复间隔和网络抖动动态延长“仍按住”的判定。
- 组合键单独加长保持时间；从组合切回单键时再快速释放已松开的那一侧。
- 进入 kb 前清空终端缓冲，避免上次命令字符被当成驾驶输入。
- 轮询约 50 Hz。

---

## 3. r 模式覆盖确认时终端无法输入 y/n

**现象**：同名轨迹覆盖提示出现后，终端吃不到 `y`/`n`。

**原因**：覆盖确认仍走普通行缓冲输入，而云端此时已切到 termios 原始模式，两者打架。

**处理**：`_await_yes_no` 改走同一套 termios 读取，SSH 下可直接按 y/n。

---

## 4. 路径回放信息丢失、fb 回不了起点、按 B 后整进程卡死

**现象**：
- 回放比 Windows 差一截，像丢了航点。
- `fb` 正走残缺后几乎回不到起点。
- r 模式录完按 B，车只退一小段，终端再无响应，只能关窗口。

**原因**（叠加）：
- 遥控模式下 `0x311` 里程经常为 0 或冻结，回放按里程推进会立刻“走完/卡住”。
- 航点超时写死，剩余距离还很大就被放弃。
- B 回程结束后 `on_complete` 不一定回调，主循环停在回程状态，键盘线程也被堵住。

**处理**：
- 回放仍以轮子里程为主（接近原 Windows 做法），`0x311` 不可用时用 `0x221` 速度积分兜底。
- 航点超时改为“剩余距离 / 当前速度”动态预算。
- 侧偏用 PID（积分 + 航向优先）纠 `w`。
- 卡死检测：里程和轮速一段时间无进展则判定堵住并退出，避免死等。
- 回放结束（含中断、超时）必须回调 `on_complete`，B 回程不再卡死终端。

精度仍是开环死推算，长距离会累积误差；建图/绝对定位接入前，这点尚未彻底解决。

---

## 5. 遥控录制：里程一直为 0，关遥控后回放仍不动

**现象**：
- r 模式用遥控器开车，终端路径和速度显示为 0。
- 按 B / `fb` 不走；关掉遥控器再跑 `fb` 仍警告并退出。
- 用户判断：回放走的是 CAN 指令，不是遥控器，遥控器优先级更高所以车不动。

**原因**：手册规定遥控器优先级最高。遥控开着时：
- 底盘忽略 CAN 的 `0x111`；
- `0x311` 里程常恒为 0。

因此“遥控录、CAN 回放”两条链路对不上：录的时候没有可用里程，回的时候指令又发不进去。

**处理**：
- `controller.py` 做融合里程计：`0x311` 在前进时优先用；冻结或缺失时，用最后一次真实值 + `0x221` 积分无缝接上，去掉约 0.5 s 的空窗。
- `agent._drive()`：发现底盘不在 `CAN_COMMAND`（待机 / 遥控）时自动发 `0x421` 切回指令模式，并清非关键故障（含遥控器失联保护）。
- 使用约定：录制可用遥控（融合里程兜底）；**回放、kb、导航必须先关遥控器**。回放不需要、也不能指望遥控器开着。

---

## 6. 方式三避障启动失败、通道名写死

**现象**：CAN 线已接上，`run_obstacle_avoidance.py` 仍失败；重启后 `can0`/`can1` 对调。

**原因**：接口检测逻辑不完整，失败提示也看不出该用哪个通道。

**处理**：
- 检测复用 `detect_socketcan_channel()`，DOWN 的接口尝试自动拉起。
- 失败时打印 `candump <实际接口>`，若用户指定通道和自动检测不一致则明确提示改参。
- 巡航速度降到 0.10 m/s、穿缝 0.07 m/s。
- 前/左/右扇区错开（前 ±30°，侧 45° 宽），避免重叠后误判成倒车。

方式三不是全局寻路：按 24 扇区点数做 cruise / spin_seek / reverse / gap_crawl，实时绕开，不停车死等。

---

## 7. 源码“失踪”、误改废弃目录

**现象**：云端/导航源文件一度只剩 `__pycache__`；之后把 Windows 参考和昨天的成果复制进工作区，容易在废弃目录里改代码。

**思路**：参考可以读，运行目录必须是新建的独立文件夹；废弃目录只作对照，最终要删。

**处理**：
- 新建 `bunker_jetson/`，把协议、控制、导航、云端、方式三、测试和说明全部落在这里。
- 不修改、不依赖 `bunker_mini_can_control/` 与子目录 `chassis_lidar_drivers/`。
- 入口脚本全部 `_bootstrap`，避免 `sys.path` 指到父目录旧包。

---

## 8. 新目录并不自包含：外部 `wrs`、旧路径、缺脚本

**现象**：删废弃目录前检查发现：
- `robosense_airy/__init__.py`、`pcap_replay.py`、`airy_driver.py`、`live_airview.py` 仍 `from wrs...`。
- `view_lidar.py` 把父目录加入 `sys.path`，会误加载旧包。
- `bringup`/`reset` 脚本还指向已不打算保留的 `drive_bunker.py`、`gs_usb_diagnose.py`。

**思路**：核心功能不得依赖外部 WRS 工程，也不得依赖即将删除的两个文件夹。3D 演示可以做成可选。

**处理**：
- 雷达包改为本地相对导入；`wrs`/`panda3d` 仅在打开 3D 视图时延迟导入。
- 补移植 `drive_bunker.py`（相对路径 + 自动检通道）。
- 重置脚本改为指向 `bringup_gs_usb_can.sh` / `socketcan_chassis.py`。
- 复制演示 pcap 到 `bunker_jetson/robosense_airy/`。
- 验证：把 `bunker_jetson` 拷到 `/tmp` 隔离目录后，入口脚本和回归测试全部通过。

旧的 `gs_usb_chassis.py` / `gs_usb_diagnose.py` / `drive_bunker_gsusb.py` 是用户态 gs_usb 方案，已被 SocketCAN 替代，不必再搬。

---

## 9. 车侧代理 CAN 打不开

**现象**：`CanOperationError: Failed to transmit: Network is down [Error Code 100]`。

**原因**：SocketCAN 接口未 UP，或通道名错。

**处理**：启动前 `ensure_socketcan_interface()`；先 `bash bringup_gs_usb_can.sh`，再用 `candump <iface>` 确认有 `0x211` 心跳。

---

## 10. 任务链对照后仍缺的能力（非本次全修）

模拟月面任务：无预设路径 → 自主探索 → 视觉找目标 → 导航靠近 → 机械臂抓取（不在本仓库）→ 返回。

当时已补、且与建图无关的部分：
- A* 全局规划（`global_planner.py`）
- 探路探索记忆（惩罚刚走过的方向）
- 视觉多帧确认（停车后连续几帧一致才认目标）
- `goto` 航向死区、大转角减速

仍依赖队友建图完成后才能做的：用真实栅格图替换伪地图、用绝对位姿替代死推算回放。这些当时按约定先不改。

---

## 11. goto / kb 小车完全不动

**现象**：云端发 `goto`、甚至 `kb`，无障碍、遥控器已关、连线正常，车仍不动。

**思路**：先排除伪地图误拒，再查 CAN 是否真通到了底盘；不要只信 `cansend` 返回 0。

**原因**（叠加）：
- 伪地图累积陈旧 occupied，`goto` 被预检拒绝。
- `--channel` 默认写成检测结果（常是 `can0`），代理当成「用户指定」跳过探测，锁在无底盘反馈的通道。
- udev 规则语法错误，USB-CAN（`can1`）DOWN / 权限不对。
- 启动瞬间 `can1` 还没广播 `0x211`，之后也不再换通道。

**处理**：
- 伪地图加时间衰减；`goto` 拒绝前用实时雷达复核。
- `--channel` 默认改为 `None`，未指定时才探测。
- 修 udev、自动 bringup、按 `0x211` 探测通道；启动后定期再探测并 `switch_channel` 热切换。
- 诊断脚本 `grep 0x211` 要容忍 `candump` 前导空格。

---

## 12. 云端雷达查看：终端 ASCII 不直观、Ctrl+C 误退云端、排版错乱

**现象**：
- `vl`/`vm`/`vb` 的点云在 SSH 终端里，人眼不直观。
- 雷达查看中按 Ctrl+C，连模拟云端一起退出；再启云端提示「没有车侧代理」（代理其实还在）。
- 终端 A 的 help / 日志换行错位（行首空格、逐行右移）。

**原因**：
- SSH 会话常无 `DISPLAY`，matplotlib 出不了工控机桌面窗口。
- `_term_keys.start()` 学 `tty.setcbreak()` 保留了 `ISIG`，Ctrl+C 变 `SIGINT` 而不是 `\x03` 字节，内部「只关查看器」的分支走不到。
- 关 ISIG 时把 `OPOST` 一并清掉，`\n` 不再变成 `\r\n`。
- 代理重连用指数退避，云端重启后还在长睡眠，像「检测不到」。

**处理**：
- `live_viewer.py` 自动探测本机 X / `XAUTHORITY`，窗口开在 Jetson 桌面。
- 明确关 `ISIG`：有查看器时 Ctrl+C 只关查看器；无查看器才退云端。
- 保留 `OPOST|ONLCR`，输入侧 raw 不变。
- 服务端 Connection refused 时把退避重置为最短，便于马上重连。

点云观感与 `robosense_airy` 不同是设计差异（原始 UDP + Panda3D 3D vs 代理抽样 + Matplotlib），不是命令下错；云端流密度后来提到 8000 点。避障走车侧原始点云，不受云端抽样影响。

---

## 13. kb 长按仍抖、轻点多走一小段：调大窗口治标不治本

**现象**：多次加长 `MIN_HOLD_S` / 释放迟滞后，长按同一键仍一顿一顿；高速下有「爆发」；轻点一下也会朝该方向多走一小会儿。组合键松开一侧后，另一侧长按会被当成无输入。

**思路**：不要再只调阈值。用脚本复现 SSH 下按键事件的真实到达形状，再看命令是阶跃还是连续；同时把「kb 警示」对照导航——两者是否共用同一条速度出口。

**原因**（两层叠加，网络延迟是上限）：
1. SSH 下 auto-repeat **不是均匀 33 ms**，而是 TCP 缓冲后的「突发 + 空白」：一批几个事件几乎同时（批内 gap≈0），批间沉默 100–800 ms。旧 `_hold_for` 只看最近 4 个 gap，批内 ≈0 把窗口缩到 `MIN_HOLD_S`，下一批空白一超就误判松开 → 停车 → 再加速。这就是「每隔一段时间收到一小段信号再小幅执行」。
2. 即使判定正确，旧 `_kb_step` 仍是 `0↔kb_v` **阶跃**，高速下就是爆发。
3. 组合键曾用「搭档最近 0.25 s 有事件」判活跃，存活键的 repeat 暂停会被误判松开。
4. 抗抖动窗口加大后，轻点也会被多锁住一小段——这是跟手性与抗抖的权衡，急停仍走 SPACE。

**处理**（软件做到这里，网络延迟消不掉）：
- `_term_keys.py`：`gap_ema` 追踪跨批次空白，保持窗口 ≥ 2.5×历史空白；组合键要求搭档**持续 repeat** 才判活跃，松开用独立的 `COMBO_RELEASE_S=0.3 s`。
- `mock_cloud.py`：目标速度与斜坡速度分开；加速 0.5 m/s²、减速 2.0 m/s²，消除阶跃。
- 结论：kb 跨笔记本↔工控机 SSH，剩余延迟是物理链路；kb 只适合低速人工接管。

---

## 14. 把 kb 警示对照导航：底盘速度出口无加速度限制

**现象 / 疑问**：kb 再抖，导航避障会不会一样致命？现有功能算完善吗？

**思路**：先画链路。kb 是「按键 → SSH → WebSocket → agent → CAN」；导航是「LiDAR 本地 UDP → 工控机计算 → CAN」，**不经过网络**，所以不会出现 kb 那种间歇性。真正同构的是：**所有驱动者最终都 `controller.set_velocity()`，旧实现直接覆盖、无加速度限制**——导航起步 `0→0.3`、避障解除后 `0→v_cmd`、转向 `-max→+max` 都是阶跃。默认速度低时不显眼，调高后就是爆发。

**处理**：
- `controller.set_velocity` 做统一斜坡（线加速度 ≤0.5 / 减速度 ≤1.5 m/s²，角 1.0 / 3.0 rad/s²），导航、避障、回放、patrol、approach、kb 的 move 全部自动平滑。
- `set_velocity_now` 直通，给 ESTOP 和运动看门狗立即停车；`stop_motion()` 语义仍是立刻停。

**仍不算完善、且主要靠建图/实测的**：里程打滑漂移（scan matching 只能缓解）、视觉反射率受光照影响、斜坡参数还要实车微调。

---

## 15. kb 组合键：松一侧，剩下那键也停（「松一键罢工」）

**现象**：WASD 和弦（如 W+A）时松掉其中一个，整车 `(0,0)` 停一下，剩下仍按着的键要等下一次连发才恢复。

**思路**：不要再加长保持窗口。先分清 SSH 终端的两种「没事件」：连发被**后按的键抢走**（对方还在按），和真的松开后出现的**安静重启间隙**。

**原因**：多数 SSH 终端只 auto-repeat **最后按下的那一键**。`motion_held()` 把被抢走连发、事件变旧的键当成点按松开，松 A 后 W 重启间隙里整车掉到 `(0,0)`。

**处理**（canonical `bunker_jetson/_term_keys.py`；`mock_cloud` 改为导入这一份，`test/`、`examples/` 下旧拷贝改成 shim）：
- 和弦成员：只要搭档还活着，本键即使自己的事件过期也仍判按住。
- 区分「连发被抢」和「安静间隙后的真松开」，间隙后再单独恢复仍按着的键。
- 从未进过和弦的孤立点按仍走 `TAP_DRIVE_S=0.18 s`。

改完需**重启 mock_cloud**。

---

## 16. 纯 kb 长按 B 刷屏，且挡住 WASD

**现象**：不在录制/回放时按住 B，终端每约 20 ms 打一次「当前不是录制/回放模式，B 键无动作」，同时车不听 WASD。

**原因**：`_kb_step` 在驾驶模式对 B 每次轮询都 `return True`，等于吃掉本拍方向键。

**处理**：纯 `kb` 里 B **静默**；只在 `record` / `play` / `fb` 上沿触发回程。WASD 不再被挡。

---

## 17. 录制、kb、遥控三条链路打架

**现象 / 疑问**：`kb` 能不能录？遥控开着录完按 B / `fb` 不动；第二次 `r` 行为含糊；遥控起步的一小段轨迹像被丢掉。

**思路**：录制线程只**采样**底盘真实运动，不负责开车。开车的是 kb 的 WASD 或实体遥控。回放必须走 CAN，和「谁录的」不是同一条权限链。

**原因**：
- `kb` 原本只驾驶、不录制；录制是 `r <名称>`。两边都在发 WASD / `(0,0)` 时会和遥控抢杆。
- 遥控占用时 idle `(0,0)` 仍触发运动看门狗，CAN 停车去抢遥控。
- 起步瞬间 `0x311` 还没有、但 `0x221` 已有速度时，旧逻辑丢样本。

**处理**：
- 遥控占用（`remoteAlive` + 模式含遥控）时 kb **不再发** WASD/`(0,0)`。
- idle `(0,0)` 不再在遥控录制时触发 `auto_stop` / CAN 停车。
- 已在录时再 `r` → 发 `fault`，不 silently 忽略。
- `_append_sample`：无里程但有速度仍落点（v/w；里程 0），遥控起步不被丢掉。
- 约定写进 `bunker_jetson/使用说明.md` §4.3：录制用 `r`+WASD（或遥控只记轨迹）；**回放必须关遥控 / SWB 到指令档**。

---

## 18. 轨迹回放贴不齐录制（kb 录为首选）

**现象**：回放相对录制时走的线有切弯、S 弯、差一截、点按被抹平；希望优先用键盘 `r` 录，再看能在哪些地方贴合实车。

**思路**：仓库里没有「录制 vs 回放」实地 RMSE。先对照源码判断回放跟的是什么，再按误差来源改高/中优先级，并单独照顾 kb 的离散按键。

**判断（回放跟的不是地面印）**：
- 录制记下的是时间戳 + 左右轮毫米 + 当时 `0x221` 的 \(v,w\)（自适应 50 ms～400 ms、50 mm；旧实现对 \(v,w\) 三点平滑）。
- 回放主路径是**里程驱动**：保持该航点速度直到两轮走完同一段 \(\Delta L,\Delta R\)；没有里程才按时开环。
- 因此纵向大约对齐轮计数，横向是死推算 + 只纠 \(w\)。十米级室内常见分米；遥控录再 CAN 放、打滑会到分米～米。**不是**厘米级重合。

**主要掉精度的点**：
1. 直行 50 mm / 弯道仍折线；匀速转弯 `|Δw|` 不再超阈值，要等到 50 mm 或 0.4 s 才采 → 切弯。
2. 纠偏瞄的是**当前航点终点**不是段内该在的点 → 被终点吸过去、和航向项打架画 S。
3. 落盘 \(v,w\) 是平滑后的 0x221，和这段实际 \(\Delta L/\Delta t\) 对不上。
4. kb 录到的 0x221 **已经过斜坡**；回放再走 `set_velocity` 等于斜两次，点按（约 0.18 s）被抹平。
5. 录放轮距不一致只告警、仍用 agent 配置去积分。
6. kb 录制不过雷达、回放过守卫：贴障碍录的路回放会被刹停。
7. 思考停顿被写成一串 `(0,0)` 航点（距离驱动下影响不大，时间回放会干等）。

**处理**（`tracker.py` / `agent._play_drive` / 轨迹 schema 2）：

录制（默认 `drive_mode=kb`）：
- 起停边沿立刻采样；持续转向按约 80 ms / 20 mm 加密。
- 松键停住后不再每 0.4 s 堆 idle 点。
- 落盘 \(v,w\) 改由相邻航点轮位移反推（默认不再三点平滑）；记下 `odometer_source`、`wheelbase_m`。

回放：
- 每段速度同样用轮位移反推，旧轨迹也受益。
- 纠偏对准段内插值位姿；横向修 \(w\)，纵向轻度加减 \(|v|\)（不改符号）。
- 回放走 `set_velocity_now`（`agent._play_drive(bypass_ramp=True)`），避免第二层斜坡。
- 优先用轨迹里的轮距积分；和 `BUNKER_WHEELBASE` 差 >2 cm 仍告警。
- kb 轨迹硬挡时先停车约 1.2 s，毛刺/过人后继续，一直挡着才整段中止。贴障碍录的路仍可能被刹——录的时候留车宽余量。

**回归**：`tests/test_tracker_quality.py`（段内插值 / 轮位移 \(v,w\) / idle 与转弯加密 / 纵向纠偏）；`test/_replay_fidelity_test.py`、`_replay_correction_test.py` 仍通过。改完需重启 mock_cloud 和 agent。

精度上限仍是轮式里程克隆，没有 SLAM 钉不住地面厘米级重合。

---

## 19. 云端 kb 多键：和弦 / 锁轴改了很多轮，本质没对上

**现象**：同时按 WASD（如 W+A）、或松开其中一个仍按着另一个，底盘像「过载」或整段丢掉还按着的那一轴。点按/松键滑行、退出 kb 刷屏等旁路修过之后，**多键老问题仍在**。对照实体遥控器，同样的斜向/松一留一并没有这个问题。

**思路**：不要再加长保持窗口或猜「还按着几键」。先问底盘和遥控器到底有没有「分辨按了几个键」这回事。

**原因**：
- 遥控器 `0x241` 每帧就是左右摇杆两个独立通道，合成一条 `(v,w)`。底盘从不数键。
- Windows 直连跟手，是因为 `keyboard.is_pressed` 读操作系统按键状态：W 和 A 可同时为真，松开立刻为假。
- SSH/终端只有字节流，通常只连发**最后一键**，且没有键松开。在工控机窗口里做和弦粘滞、`drive_stick` 锁轴，仍是从「被抢走的字符流」反推，短窗则丢和弦、长窗则溜车——两条都治不好。

**处理**：
- 驾驶意图改在**物理键盘所在的那台机器**采样 HID，按遥控器方式每拍送双轴，不再在 SSH 里猜字符。
- 先做 `hid_kb_client.py`（WebSocket 灌 `kb_stick`）；随后改为更短的 TCP 路径（§20）。
- 工控机 SSH 里的 `kb` 只保留单键兜底，并明确提示多键请走本机客户端。

---

## 20. 遥控延迟 50–90 ms / 看起来只有 10–20 Hz：SSH 不是控制总线

**现象**：联调方问「是不是还在走 SSH、没走直连 TCP」；体感约 50–90 ms，刷新像 10–20 Hz；认为 Wi-Fi 6 上 TCP 应不超过约 20 ms，50 Hz 已经偏慢。

**思路**：先把各 hop 拆开再测，不要把状态栏刷新当成操作延迟；再单独做一条「笔记本 HID → 工控机 TCP → CAN」的服务，绕开 SSH 字符流和 WebSocket JSON。

**实测**（工控机本机，不下发非零速度）：
- 新协议 TCP 本机 ping：中位 **0.06–0.09 ms**。
- 底盘 `0x221`：中位 **20.00 ms**（电机环就是 50 Hz，改不了）。
- 当时 `:9100` / mock_cloud 未在听，笔记本→工控机的实车往返尚未采到。
- 旧云端 kb 的 10–20 Hz 来自 `KB_REFRESH_S=0.10 s`，不是 Wi-Fi。

**处理**：
- `bunker_mini/teleop_tcp.py`：12 字节头二进制帧；stick **100 Hz 发、不等每帧 ACK**（避免 Wi-Fi RTT 卡频率）；断流约 120 ms 停车。
- agent 默认听 `0.0.0.0:9100`，stick 走 `set_velocity_now`（不斜坡、不过雷达）。
- 笔记本客户端 `teleop_tcp_client.py`；只要遥控、不开完整 agent 可用 `systemd/bunker-teleop.service`（**不可与 agent 同时抢 9100/CAN**）。
- 测延迟：`python3 measure_latency.py`。
- 结论：软件能拿掉的是 SSH / JSON / 10 Hz 刷新；叠 Wi-Fi RTT + 等下一拍 CAN，局域网体感大约 15–35 ms。`m` / 录放 / 任务链仍走原通道，不会 magically 变快。

---

## 21. `teleop_tcp_client` 在工控机上跑：还没连上 TCP 就退出

**现象**：在 Cursor 远程终端执行  
`python teleop_tcp_client.py --host 59.79.233.120`，报  
`keyboard` 要 root、`evdev`/`pynput` 未装，进程退出码 2。

**思路**：先看报错发生在读键还是连网。客户端在 `HidStick.open()` 失败时直接 return，**还没有对 9100 发起连接**。

**原因**：
- 客户端必须跑在**手正在按的那台电脑**上。工控机 / SSH / Cursor 远程里的 `keyboard` 读的是 `/dev/input`，Linux 普通用户要 root；即便 sudo 读通，也读不到笔记本经 SSH 传过来的键。
- `--host 59.79.233.120` 是给笔记本连工控机用的；人已经在工控机上时填公网 IP 没有意义。公网还要确认 **9100** 已做端口映射（只开 22 不够）。
- 当时 agent 未重启，`:9100` 空着；`bunker-teleop` 也未安装。代码在、实车手感还没走通。

**处理（用法，不是再改协议）**：
1. 工控机重启 agent（日志应有 `teleop TCP 已监听 0.0.0.0:9100`）。
2. **笔记本本机**（Windows 一般 `pip install keyboard`）运行  
   `python teleop_tcp_client.py --host <工控机可达 IP>`。
3. 不要在 SSH 窗口按 WASD 当 TCP 遥控；旧 `kb` 与 TCP 是两条路。
4. 工控机上不要再跑 `teleop_tcp_client.py`。

---

## 22. 老师要 100Hz / ≤20ms：车侧改对了，Cursor 里 `kb` 仍不是控制通道

**现象**：按老师要求（不要用 SSH 当遥控总线、同网 Wi-Fi 6 TCP 应数毫秒到十几毫秒、50Hz 已慢、目标 100Hz）改完车侧后，在 Cursor 远程终端敲 `kb`，仍是 50–90 ms、一顿一顿、斜向飘。

**思路**：把「发送循环 100Hz」和「按键状态多久更新一次」拆开。本机 `127.0.0.1:9100` 通常 &lt;1 ms；慢的是前面：笔记本键 → Cursor/SSH 字符流（无 key-up）→ `TermKeyReader` 用连发猜按住。循环只是在空打已经猜好的 `(v,w)`。

**处理**（车侧对齐老师，不靠再调 hold 窗）：
- `0x111` 周期 20 ms → **10 ms**；stick 看门狗收紧；`set_velocity_now` 不再每次 `+0.20 s` deadline。
- 去掉驾驶路径上的 0.58 s / 0.70 s「猜还按着」窗口；TTY 静默超过约 50 ms 双轴清零。
- `run_local.py` 横幅不再写「本终端 100Hz」——那是误导。Cursor 里 `kb` 只标成调试。
- stick 默认不 ACK；`cmd` 异步，避免 100Hz 被任务 ACK 卡住。

**没解决的**：SSH 没有 key-up，首击连发延迟 500–660 ms，同一时刻通常只连发最后一键。再加大 `TELEOP_LIVE_S` 只会更溜车。

---

## 23. 「拷脚本到笔记本开 PowerShell」用起来不通

**现象**：为了 HID，增加 `teleop_from_laptop.py`，要求拷到笔记本本机跑、`--host` 填工控机 IP。操作麻烦；笔记本本地仓库里也没有 `run_local.py`；客户端连 `:9100` 被拒绝。

**思路**：不要再在「笔记本必须再跑一个 Python、再直连 9100」上打补丁。先画清**谁在听、哪张网通、键盘在哪**。

**原因**（拓扑，不是协议又写错）：
- Cursor 打开的是工控机硬盘。笔记本本地那份仓库可以没有 `run_local.py`，也不该在笔记本上起服务端。
- 工控机两张网：Wi-Fi `59.79.233.120`（SSH 进来）、网线 `192.168.1.102`（雷达局域网）。笔记本不在 192.168.1.0/24；横幅把两个 IP 都打出来，`--host` 很容易填错。
- 当时对外只保证 **22**。9100 没人听、填 `127.0.0.1` / 雷达网 IP、或校园网禁客户端互访，都会 **Connection refused**。工控机 ping 笔记本已丢包。
- HID 只能读「键插在哪台机器」。Jetson 上没有 USB 键盘（只有 `gpio-keys` / HDMI 声卡事件），在远程终端里跑 HID 读不到笔记本键。

**处理（跳出「再暴露 9100」）**：
- 老师批的是 **SSH 伪终端当遥控总线**（字符、10–20Hz），不是禁止走已经打通的 SSH **端口转发**（那是字节管道，不限 10–20Hz）。
- 完整用法：工控机只跑 `run_local.py`；驾驶页 / WebSocket 走 `:9101`（`teleop_web.py`）。笔记本用**本机浏览器**打开（直连工控机 IP，或经 Cursor 把 9101 转到 `127.0.0.1`）。浏览器的 `keydown`/`keyup` 等同 HID，无需拷文件、无需本机 Python、无需笔记本仓库里有 `run_local.py`。
- 笔记本不应再去连 `:9100`。`:9100` 给本机命令台；局域网直连 9100 不再当默认验收路径。
- 诚实对照老师：频率可以 100Hz；端到端是「SSH RTT + 一帧」或「Wi-Fi 直开 9101」。若他按「无线直连、完全不经 SSH」验收，隧道方案仍可能略超 20 ms。

---

## 24. 网页已连接、100Hz，底盘仍无响应（2026-08-22～23）

**现象**：笔记本 Chrome 打开 `http://<工控机IP>:9101`，绿字「已连接」、约 100Hz、RTT 数毫秒，工控机已收 stick；终端一直 `mode=?`、电量 0，车不动。输入法、刷新、重启 `run_local.py` 都不解决问题。

**思路**：先拆链路，不要把网页状态当成底盘锁定。手册：[BUNKER MINI 2.0](https://agilexsupport.yuque.com/staff-hso6mo/rg519a/bbv1zm1aqm0ifyab?singleDoc#) 规定底盘上电后 **0x211 每 200ms 自广播**；未开遥控默认待机，须周期发 `0x421=0x01`（20ms，超时 500ms）后 `0x111` 才生效。频率对不上只会造成操控发飘，不会整车无响应。

**中间弯路（软件确有问题，但不是最终根因）**：
- 探测结束后 listen-only 没显式关掉 → `0x111` 发不出去；网页一度误以为「已连到底盘」。
- 无 `0x211` 时仍空闲发 `0x421` / 周期 down/up 重探 → USB-CAN 发送邮箱堵死（`cansend` 成功、内核 TX 不涨、`Transmit buffer full`）；按键帧被自己挤掉。
- 页面失焦 / 重连发完整急停，日志刷 `EMERGENCY STOP`，像有进程抢杆；实际只有 `run_local` + agent。
- 处理：空闲不发保活；按键才按手册发 `0x421+0x111`；启动清一次发送邮箱；失焦改停车不急停；网页写明「已连接 ≠ 锁定 0x211」。

**根因**：清掉上述自扰之后，`candump can0/can1` 在 125k～1M 上仍无任何帧。手册：尾部电压 **低于 24V 为低电、低于 25V 应充电**；欠压故障在 `0x211` byte[5]。电压掉到控制器/BMS 保护关机时，CAN 节点整段消失——不是频率、不是网页、不是通道锁错。充电后 `0x211` 恢复，遥控正常。

**记住**：`mode=?` 先看尾部电压表，再查软件。网页通只说明笔记本→工控机。

---

## 25. 网页/工控机已通、`can0` 也 UP，仍无 0x211（2026-08-24）

**现象**：
- 笔记本 `:9101` 绿字已连接、约 100Hz、RTT ~2 ms，工控机已收 stick，车不动；页上曾显示 `can1 · 无反馈`。
- `bash bringup_gs_usb_can.sh` 报 `USB-CAN not found`。
- 插回适配器后 `check_can.py`：`1d50:606f` 在、`can0` UP / ERROR-ACTIVE，但听 2 s 仍 0 帧。
- 用户指出：8 月 23 日还能 CAN 遥控，不可能是「8 月 21 日起硬件就坏」；电压正常、接线没问题，否则工控机也开不了机。

**思路**：先拆「谁占口 / 绑的哪张网卡 / 帧有没有上线」，不要用内核日志日期当结论（本机时钟乱跳，`who -b` 写 8 月 21、uptime 只有数小时）。判定只看 **此刻** 能不能听到底盘广播，不要信 `cansend` 返回 0，也不要把工控机有电当成 CAN 总线有对端。

**中间弯路**：
- 启动听不到 `0x211` 就对 USB-CAN 做 `usbreset` / `authorized` 软复位 → 适配器一度从总线消失；只剩板载 `can1`（mttcan）时 agent 绑上去狂发 `0x111` → `can1` 连续 bus-off。
- 网页 100Hz 零速 / 按键在无 ACK 时仍发 `0x111` → `Transmit buffer full`，再 `ip link down/up` 清邮箱时内核 `gs_usb can0: usb xmit fail`。
- 一度把「无帧」全归因于欠压（§24）。用户反驳后复测：`cansend` 成功但 **内核 TX 一直 0**，并打出 `usb xmit fail`——这是 USB 发包失败，不是底盘没电。
- 工控机供电 ≠ 底盘 CAN 节点；`lsusb` 有设备、`can0` UP ≠ USB 数据管道还能收发。

**根因**：candleLight 固件 / gs_usb USB bulk 发送（及伴随的接收 URB）卡死。枚举还在，帧到不了 CAN 控制器，工控机也听不到一直在广播的 `0x211`。`ip link` / `usbreset` 只重置主机侧，MCU 没有冷启动。

**处理**：
- **有效恢复**：物理拔掉 candleLight，等 3 秒再插回（USB 掉线 + 多数情况下 VBUS 掉电，MCU 冷启动）。本次设备号 008→009，`can0` RX 立刻上万，2 s 内听到 `0x241/0x221/0x311/0x252/0x253`。
- 不要再对「仅仅听不到 0x211」做软件 `usbreset`。
- 未指定通道时：USB-CAN（即使 DOWN）优先于板载 mttcan；没有 USB 口时不要把 `0x111` 打到板载 CAN。
- 未听到 `0x211` 时运动帧保持静默，避免再次堵死 gs_usb。
- 本机检测：`python3 check_can.py`（`run_local` 里敲 `can`）。看 USB 归属、RX/TX/TXdrop、是否 `usb xmit fail`、2 s 内有无底盘帧。有 `0x211` 一类帧再开 `run_local.py`。

**记住**：再出现「网卡正常、听不见 0x211、还有 `usb xmit fail` / TXdrop」→ 先拔插适配器，不要改探测逻辑、也不要先怪电压。

---

## 26. goto 只到点、fb 回起点姿态差（2026-08-24）

键盘 HID（笔记本网页 / `teleop_from_laptop.py` → TCP `:9100`）已趋于可用之后，剩下两条任务链：点到点导航不管车身；轨迹往返尤其 `fb` 回到起点时角速度对不上。

**思路**：不要再调守卫或键盘看门狗。对照 `navigator.py` 的到达条件和 `tracker.py` 里 `reverse=True` 实际走的是哪条回放路径；`bypass_guard` 只应关雷达，不该连精度功能一起关。

### A. `goto` 位置近似到达，经常忽略车身

**现象**：小车会开到地图上某个点附近就报到达，车头常常是歪的；大转角时斜着蹭进圆圈，不像先转正再走。

**原因**：
- 到达只看中心距目标 ≤ 15 cm，**不看航向**。XY 到位后没有原地转正。
- 航向差很大时线速度仍保留 25%（`turn_min_speed_ratio`），车身斜着进圈。
- 距目标 40 cm 且里程几乎不动，就会被当成到达（碎石/打滑兜底写得过宽），空地上经常在半个车身外报 `arrived`。

**处理**（`navigator.py` / `agent.py` / `run_local.py`）：
- 航向误差超过约 23°：线速度清零，**原地转正再走**。
- XY 到位后第二段：按接近航向（或载荷里的 `yawDeg`）原地转到约 5° 内；超时约 4 s 才接受当前朝向。
- 到达半径收到 8 cm；停滞判定只在约 16 cm 内，终点还要求航向大致对齐。
- 控制台可选：`g <x> <y> [speed] [yawDeg]`。

中间航点仍只过位置（折线途中不必每段转正）。长距离仍是轮式里程，建图/绝对定位接入前会漂。

### B. 轨迹回放尤其 `fb`：精度低，回起点角速度明显不对

**现象**：`f` 已经有偏差；`fb` 回程更差，**回到出发位置时**角速度分配和当时真实转弯对不上。

**原因**（叠加，`fb` 最重）：
1. `track_follow` 一律 `bypass_guard=True`（已录路径不过雷达）。旧代码把这个标志和「精度功能」绑在一起：关掉 `segment_vw`（轮位移反推段速度）、闭环纠偏、末端爬行停靠。`f` 也只用航点快照。
2. `reverse=True`（`fb` / B 回程）再早退到 **`_play_recorded_timeline`**：按时间戳重放取反后的快照 `(v,w)`，**不等两轮走完那段弧**。底盘稍慢/稍快，终点位置和航向就漂。
3. 回程最后几段对应录制开头（起步、点按、残余 `w`）。时间轴只按时钟播这些快照，回到起点时角速度和真实轮弧对不上。
4. 旧纠偏不敢给倒车开：纵向误差在倒车时加减反了；`|v|` 地板成 ±0.02 会把抖动的微小正速度抬成往前爬。这是当年改走开环时间轴的直接原因。

**处理**（`tracker.py`）：
- `bypass_guard` **只关雷达**，不再关 `segment_vw` / 纠偏 / 停靠。
- `fb` 与 `f` 同一套：**轮程闭环 + 段速度从相邻航点轮位移反推**。`reversed()` 后 delta 为负，`v<0`、曲率与去程一致；轮差抖动若积成反号，才回退已取反的快照。
- 倒车纠偏：横向项反向（倒车偏左再右转会更偏）；纵向「还没倒够」加大 `|v|`；去掉会翻成前进的 0.02 地板。
- 倒车段剩余距离取正，终点最后约 25 cm 减速爬行再停稳。
- 无 `0x311` 时仍退回时间轴，文案标明位置只是近似。

**回归**：`test/_nav_turn_test.py`（原地转正、0.35 m 停滞不冒充到达）、`test/_navigator_waypoints_test.py`、`tests/test_reverse_timeline.py`（回程走里程不是时间轴）、`test/_replay_fidelity_test.py`、`_replay_correction_test.py`、`test/_smoke_test.py`。改完需**重启 `run_local.py`**（网页 / 笔记本 HID 客户端也要重开）。

精度上限仍是轮式里程克隆，没有 SLAM 钉不住地面厘米级重合。

---

## 对照：文件落点

| 问题域 | 主要文件 |
|---|---|
| 融合里程 / CAN 探测与热切换 / 速度斜坡 | `bunker_jetson/bunker_mini/controller.py`、`can_util.py`、`agent.py` |
| 键盘 / y/n / B 回程 / kb 斜坡 / 和弦松一键 | `bunker_jetson/_term_keys.py`、`mock_cloud.py` |
| TCP 遥操 / HID 双轴 / 延迟测量 | `bunker_jetson/bunker_mini/teleop_tcp.py`、`teleop_tcp_client.py`、`teleop_tcp_server.py`、`_hid_stick.py`、`measure_latency.py`、`systemd/bunker-teleop.service` |
| 本地控制台 / 去掉猜键 / 网页遥控 | `run_local.py`、`teleop_web.py`（`:9101`）、`teleop_from_laptop.py`（备选，不再当默认） |
| 网页已通但车不动 / 发送队列 / 低电 | `can_util.py`、`controller.py`、`agent.py`、`teleop_web.py`；判定只看 `0x211` / 尾部电压 |
| USB-CAN 卡死 / 拔插恢复 / 本机检测 | `check_can.py`、`run_local.py`（`can`）、`can_util.py`、`agent.py`、`controller.py`、`bringup_gs_usb_can.sh` |
| 录制回放 / 段内纠偏 / kb 录制采样 | `bunker_jetson/bunker_mini/tracker.py`、`agent.py` |
| goto 车身姿态 / fb 里程闭环回程 | `navigator.py`、`tracker.py`、`agent.py`、`run_local.py` |
| 方式三避障 | `bunker_jetson/robosense_airy/obstacle.py`、`run_obstacle_avoidance.py`、`socketcan_chassis.py` |
| 导航规划 / 伪地图衰减 | `navigator.py`、`patrol.py`、`occupancy.py`、`global_planner.py` |
| 雷达桌面查看 | `live_viewer.py`、`mock_cloud.py` |
| 自包含导入 | `_bootstrap.py`、`robosense_airy/__init__.py` |
| 回归 | `bunker_jetson/tests/`、`bunker_jetson/test/` |

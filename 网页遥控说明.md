# 网页遥控实操（run_local）

工控机起服务 → **笔记本浏览器**连工控机开车。详细联调见 [`使用说明.md`](使用说明.md)。

---

## 一、先搞清 IP（别填错）

| 设备 / 用途 | IP | 谁用来连 |
|-------------|-----|----------|
| **Jetson 工控机（实验室 Wi‑Fi）** | **`59.79.233.120`** | 笔记本浏览器、SSH、遥控 |
| 工控机有线网口（接雷达那根网线） | `192.168.1.102` | 仅工控机 ↔ 雷达，**笔记本不要填这个** |
| RoboSense Airy 雷达 | `192.168.1.200` | 雷达 MSOP，**不是工控机遥控地址** |

笔记本和工控机都在 **同一实验室局域网（Wi‑Fi）** 时，遥控地址固定是：

```text
http://59.79.233.120:9101
```

`192.168.x.x` 是雷达专用网段；用它在笔记本上打开遥控页 **连不上**。

---

## 二、笔记本连接工控机（完整步骤）

### 2.1 网络

1. 笔记本连实验室 **Wi‑Fi**（与 Jetson 同一网，不是手机热点 unless 工控机也在上面）
2. Windows：设置 → 网络和 Internet → 看当前 Wi‑Fi 已连接
3. **不要**指望用 `192.168.1.102` 或 `192.168.1.200` 打开遥控页

### 2.2 确认能 ping 通（可选但建议）

笔记本 **PowerShell** 或 CMD：

```powershell
ping 59.79.233.120
```

- 有回复（如 `来自 59.79.233.120 的回复`）→ 网络通，可继续  
- **请求超时** → 先解决 Wi‑Fi / 是否同网，不要开浏览器硬试

### 2.3 工控机先起服务

在 **Jetson 工控机** 开一个终端（SSH 进去或站在车旁操作均可）：

```bash
cd ~/Desktop/chassis_lidar_drivers
# 若路径不同，改成你仓库实际位置
python3 run_local.py
```

等到终端出现类似：

```text
网页控制台已开。笔记本浏览器打开：
  http://59.79.233.120:9101
```

**这个终端要一直开着**；`Ctrl+C` 会急停并关掉整套服务。

若提示 `:9100` 已被占用：

```bash
pkill -f 'run_agent.py'
pkill -f run_local.py
python3 run_local.py
```

### 2.4 笔记本打开控制台

1. 打开 **Chrome** 或 **Edge**（用笔记本 **本机** 浏览器，不要只在 Cursor 里看代码）
2. 地址栏输入并回车：

   ```text
   http://59.79.233.120:9101
   ```

3. 出现登录页 → 输入控制台密码（默认 `fafu123456`；工控机可用环境变量 `BUNKER_WEB_PASSWORD` 修改）
4. 进入 **BUNKER 本地控制台**

若 IP 以后变了，以 `run_local.py` 启动时终端打印的为准，**仍以 Wi‑Fi 那个为准，不是 192.168 那个**。

### 2.5 开始遥控

1. **鼠标点一下页面空白处**（让页面获得键盘焦点）
2. 输入法切 **英文**
3. 点顶栏或左侧 **「遥控 WASD」**
4. 按住 **W / S / A / D** 开车，**松开即停**
5. 确认顶栏：**已连接**、**遥控 Hz ≈ 100**、「工控机已收 v=… w=…」会随按键变化

| 键 | 作用 |
|----|------|
| W S A D | 前 / 后 / 左 / 右 |
| + − | 调速度档 |
| 空格 | 急停 |
| 顶栏 ESTOP | 急停 |

### 2.6 SSH 和遥控是两回事

- **SSH**（如 Cursor 远程桌面 / `ssh fafu_robot@59.79.233.120`）：改代码、在工控机终端里跑 `run_local.py`
- **遥控**：笔记本浏览器打开 **`http://59.79.233.120:9101`**，用 WASD 开车  

**不要在 SSH 终端里输入 `kb` 当正式遥控**（只有约 10～30 Hz，达不到网页遥控要求）。

### 2.7 常见连接错误

| 现象 | 处理 |
|------|------|
| 浏览器打不开页 | 工控机是否已跑 `run_local.py`；是否用了 `192.168.x.x` 而不是 `59.79.233.120` |
| 能开页但「未连接」 | 刷新；确认工控机终端没报错、9101 没被别的程序占 |
| ping 不通 | 笔记本与工控机是否同一 Wi‑Fi；工控机 Wi‑Fi 是否在线 |
| 已连接、Hz 正常但车不动 | 见下文 **第六节**（CAN / 遥控器等） |

**不要同时开**：第二个 `run_local.py`、`bunker-teleop.service`、旧云端 `run_mission.py`。

---

## 三、工控机首次启动

```bash
cd ~/Desktop/chassis_lidar_drivers
pip3 install -r bunker_jetson/requirements.txt   # 仅首次
python3 run_local.py
# USB-CAN 未起来：python3 run_local.py --bringup
```

- 底盘电压 **< 24 V** 先充电再跑

---

## 四、页面速查

顶栏全局有效：连接、Hz、RTT、**急停**。

| 页 | 干什么 |
|----|--------|
| 遥控 WASD | 键盘 / 触控方块开车 |
| 任务 / 导航 | move（速度×时间）、goto（坐标，需雷达）、fo 探路 |
| 轨迹 | 录制 → WASD 开 → 停止 → 回放 |
| 雷达 / 地图 | **雷达开**；点云窗在 **工控机桌面** 弹出 |
| 底盘状态 | 电量、CAN、模式、故障 |
| 事件 | 任务 / 故障日志 |

**WASD 任意页都有效**；光标在输入框里时不会开车。

---

## 五、常用任务

| 要做的事 | 去哪 | 要点 |
|----------|------|------|
| 慢速试车 | 遥控 WASD | 先低速，松键即停 |
| 走固定距离 | 任务 → move | 填 v、w、秒数 |
| 走到坐标点 | 任务 → goto | **先雷达开** |
| 探路 | 任务 → fo | 填目标名 |
| 录轨迹 | 轨迹 | 填名 → 录制 → WASD → 停止 |
| 回放 | 轨迹 | 选轨迹 → 回放 |
| 取消 | 任务 → 取消 | 或直接 **WASD** 接管 |

原点 = 启动 `run_local` 时车位；**重置原点** = 当前位置变 (0,0)。

---

## 六、急停

| 方式 | 操作 |
|------|------|
| 硬件 | 底盘尾部红色蘑菇头 |
| 软件 | 空格 或 顶栏 ESTOP |
| 松键 | 松开 WASD |
| 关页 | 关标签 / 切应用 → 自动停车 |

---

## 七、车不动 / 连不上（按顺序查）

1. 笔记本 ping `59.79.233.120` 是否通  
2. 浏览器地址是否为 **`http://59.79.233.120:9101`**（不是 192.168）  
3. 工控机 `run_local.py` 是否在跑  
4. 顶栏 **已连接**；英文输入法 + 点过页面空白  
5. **底盘状态**：CAN 是否通；物理遥控器是否关着  
6. 工控机：`python3 check_can.py`

---

## 八、备选：不用浏览器

笔记本 **本机 PowerShell**（非 Cursor 远程终端）：

```powershell
python teleop_from_laptop.py --host 59.79.233.120
```

工控机仍只跑 `run_local.py`。浏览器和脚本 **不要同时按 WASD**。

---

## 九、工控机终端命令（可选）

同一 `run_local` 窗口可输：`fo` `goto` `move` `cancel` `r` `f` `lo` `query` `can` `h`  
与网页按钮等价；**不要用 `kb` 当正式遥控**。
